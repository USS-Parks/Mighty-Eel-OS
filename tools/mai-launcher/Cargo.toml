[package]
name = "mai-launcher"
description = "MAI front-door launcher: Win32 splash + terminal interface + mai-api supervisor"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
# Renders as `mai.exe` — the front-door binary users click to open
# the program. Distinct from `mai-api.exe` (headless service) and
# `mai-admin.exe` (operator tooling).
name = "mai"
path = "src/main.rs"

[dependencies]
anyhow = "1"
image = { version = "0.25", default-features = false, features = ["png"] }

[dependencies.windows]
version = "0.58"
features = [
    "Win32_Foundation",
    "Win32_Graphics_Gdi",
    "Win32_System_Console",
    "Win32_System_LibraryLoader",
    "Win32_UI_WindowsAndMessaging",
]

# Override the workspace `unsafe_code = forbid` because the splash
# module necessarily uses Win32 calls. Everything else inherits the
# workspace lints (clippy::all + clippy::pedantic as warn).
[lints.rust]
unsafe_code = "allow"

[lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
missing_errors_doc = "allow"
missing_panics_doc = "allow"
module_name_repetitions = "allow"
must_use_candidate = "allow"
doc_markdown = "allow"
too_many_lines = "allow"
cognitive_complexity = "allow"
wildcard_imports = "allow"
