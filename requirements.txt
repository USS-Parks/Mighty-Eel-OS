# mai runtime requirements (human-readable, version ranges)
# Source of truth: pyproject.toml [project.dependencies]
# For reproducible installs use requirements-lock.txt instead.
# See docs/LOCK-FILE-POLICY.md for the regeneration command.

httpx>=0.27
pydantic>=2.0
structlog>=24.0
