[package]
name = "fabric-token"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "The WSF trust-token primitive: issue, verify, attenuate, and meter budget-carrying capability tokens"

[dependencies]
fabric-contracts = { path = "../fabric-contracts" }
fabric-crypto = { path = "../fabric-crypto" }
fabric-proof = { path = "../fabric-proof" }
serde_json = { workspace = true }
thiserror = { workspace = true }
chrono = { workspace = true }
hex = "0.4"

[dev-dependencies]
tokio = { workspace = true }

[features]
# Quarantined adversarial regression harness (tests/security_regression.rs).
# Off by default so those tests — which assert current vulnerable behavior — do
# not run in the product suite; enable with --features security-regression.
security-regression = []

[lints]
workspace = true
